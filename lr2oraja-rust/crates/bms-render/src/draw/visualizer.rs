// Visualizer draw logic stubs.
// These require runtime game state (note positions, timing data, etc.)
// which will be available in Phase 11. For now, provide empty implementations.

/// Stub: compute BPM graph pixel data.
pub fn compute_bpm_graph_pixels(_width: u32, _height: u32) -> Vec<u8> {
    vec![]
}

/// Stub: compute hit error visualizer pixel data.
pub fn compute_hit_error_pixels(_width: u32, _height: u32) -> Vec<u8> {
    vec![]
}

/// Stub: compute note distribution graph pixel data.
pub fn compute_note_distribution_pixels(_width: u32, _height: u32) -> Vec<u8> {
    vec![]
}

/// Stub: compute timing distribution graph pixel data.
pub fn compute_timing_distribution_pixels(_width: u32, _height: u32) -> Vec<u8> {
    vec![]
}

/// Stub: compute timing visualizer pixel data.
pub fn compute_timing_visualizer_pixels(_width: u32, _height: u32) -> Vec<u8> {
    vec![]
}
