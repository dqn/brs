/// Main application controller managing game states and resources.
pub struct MainController;

impl MainController {
    pub fn new() -> Self {
        Self
    }
}

impl Default for MainController {
    fn default() -> Self {
        Self::new()
    }
}
